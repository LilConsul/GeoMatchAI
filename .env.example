# GeoMatchAI Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# Required Configuration
# ============================================================================

# Mapillary API Configuration
# Get your client token from: https://www.mapillary.com/dashboard/developers
MAPILLARY_API_KEY=your_mapillary_api_token_here

# ============================================================================
# Optional Configuration
# ============================================================================

# Logging Configuration
# Sets the logging verbosity level for the application
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Default: INFO
#LOG_LEVEL=INFO

# Device Configuration
# Specify which device to use for computation
# Options:
#   - auto: Automatically detect (uses CUDA if available, else CPU)
#   - cuda: Force CUDA/GPU usage (will fail if not available)
#   - cpu:  Force CPU usage
# Default: auto
#DEVICE=auto

# CUDA Device Selection (for multi-GPU systems)
# Specify which GPU(s) to use (comma-separated for multiple GPUs)
# Examples:
#   CUDA_VISIBLE_DEVICES=0       # Use only GPU 0
#   CUDA_VISIBLE_DEVICES=0,1     # Use GPUs 0 and 1
#   CUDA_VISIBLE_DEVICES=1,2,3   # Use GPUs 1, 2, and 3
# Default: All available GPUs
#CUDA_VISIBLE_DEVICES=0

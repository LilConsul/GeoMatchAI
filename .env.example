# GeoMatchAI Development Environment Configuration
#
# ⚠️  NOTE: This file is ONLY for development/testing convenience!
# ⚠️  When using GeoMatchAI as a library, configure it in your code instead:
#
#     from geomatchai import config
#     config.set_mapillary_api_key("your_key")
#     config.set_log_level("DEBUG")  # optional
#     config.set_device("cuda")      # optional
#
# Copy this file to .env for development/testing purposes only.

# ============================================================================
# Development Configuration (Optional - for convenience only)
# ============================================================================

# Mapillary API Configuration
# Get your client token from: https://www.mapillary.com/dashboard/developers
# In production: Use config.set_mapillary_api_key("your_key") instead
MAPILLARY_API_KEY=your_mapillary_api_token_here

# Logging Configuration (Optional)
# In production: Use config.set_log_level("DEBUG") instead
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
#LOG_LEVEL=INFO

# Device Configuration (Optional)
# In production: Use config.set_device("cuda") instead
# Options: auto, cuda, cpu
#DEVICE=auto

# CUDA Device Selection (for multi-GPU systems)
# This is standard PyTorch behavior, not library-specific
# Examples:
#   CUDA_VISIBLE_DEVICES=0       # Use only GPU 0
#   CUDA_VISIBLE_DEVICES=0,1     # Use GPUs 0 and 1
#CUDA_VISIBLE_DEVICES=0
